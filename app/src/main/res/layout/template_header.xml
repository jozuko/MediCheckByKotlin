<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.studiojozu.medicheck.resource.custom.ShrinkButton
        android:id="@+id/button_menu"
        style="@style/SmallButtonLeftStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableLeft="@mipmap/ic_arrow_left"
        android:drawableStart="@mipmap/ic_arrow_left"
        android:text="@string/button_menu"/>

    <TextView
        android:id="@+id/header_text"
        style="@style/MgenFontMedium"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@android:color/black"
        android:textSize="@dimen/menu_text_size"/>

    <ImageView
        android:id="@+id/header_icon"
        android:layout_width="@dimen/menu_icon_size"
        android:layout_height="@dimen/menu_icon_size"
        android:layout_centerVertical="true"
        android:layout_marginRight="4dp"
        android:layout_toLeftOf="@id/header_text"/>

</RelativeLayout>
