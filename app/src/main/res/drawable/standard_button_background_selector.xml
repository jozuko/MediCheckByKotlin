<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ボタンの背景画像(押下時) -->
    <item
        android:drawable="@drawable/standard_button_background_pressed"
        android:state_pressed="true"/>

    <!-- ボタンの背景画像(通常時) -->
    <item android:drawable="@drawable/standard_button_background_normal"/>

</selector>
